<script type="text/javascript">
  console.log('Hello World! index loaded');
</script>

<link rel="stylesheet" href="{{ asset('bundles/socialsweetrecruitment/css/bootstrap.min.css') }}"/>
<link rel="stylesheet" href="{{ asset('bundles/socialsweetrecruitment/css/main.css') }}"/>

<script src="{{ asset('bundles/socialsweetrecruitment/js/jquery-3.2.1.min.js') }}"></script>
<script src="{{ asset('bundles/socialsweetrecruitment/js/bootstrap.min.js') }}"></script>

<div>
       {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
           <h1> Logged In As: {{ app.user.username }} </h1>
           <a href="{{ path('fos_user_security_logout') }}">
                 <h1> Logout </h1>
           </a>
       {% else %}
          
        <div style=" position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%);">
            <a class="btn btn-primary" href="{{ path('fos_user_security_login') }}">Login</a>
            <a class="btn btn-danger" href="{{ path('fos_user_registration_register') }}">Create New User</a>
        </div>

       {% endif %}

</div>

{% if app.request.hasPreviousSession %}
       {% for type, messages in app.session.flashbag.all() %}
           {% for message in messages %}        
                <div class="flash-{{ type }}">
                    {{ message }}    
                </div>
           {% endfor %}
       {% endfor %}
{% endif %}
<div>

       {% block fos_user_content %}
       {% endblock fos_user_content %}

</div>